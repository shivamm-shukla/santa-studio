python-dotenv>=1.0.0
anthropic>=0.40.0
google-genai>=0.1.0
groq>=0.9.0
requests>=2.31.0
openai-whisper
static-ffmpeg
moviepy>=2.0.0
pydub
gTTS>=2.5.0
streamlit>=1.60.0
fastapi>=0.110.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Heavy local voice cloning - install manually when ready to test:
# pip install coqui-tts torchaudio
# (needs a real ~6s voice sample; see providers/voice/xtts_provider.py
# for the CPML commercial-license note before using this on a monetized channel)
