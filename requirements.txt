python-dotenv>=1.0.0
anthropic>=0.40.0
google-genai>=0.1.0
groq>=0.9.0
requests>=2.31.0
openai-whisper
static-ffmpeg
moviepy>=2.0.0
pydub
gTTS>=2.5.0
streamlit>=1.60.0
fastapi>=0.110.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Voice cloning (the default voice provider). Pulls torch + a ~1.9GB model
# on first run; needs a real ~6s voice sample. See
# providers/voice/xtts_provider.py for the CPML commercial-license note
# before using this on a monetized channel.
# The [codec] extra is required from torch 2.9 on (torchcodec audio IO).
coqui-tts[codec]>=0.27
# coqui-tts 0.27 still imports transformers.pytorch_utils.isin_mps_friendly,
# which transformers 5.x removed.
transformers>=4.57,<5
